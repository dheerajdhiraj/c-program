#include<stdio.h>
int main()
{
	int size,i;
	printf("enter the size of array");
	scanf("%d",&size);
	int arr[size]
	{
	printf("enter the element of array");
	for(i=0;i<size;i++)
	{
		scanf("%d",&arr[i]);
	}}
	for(int i=0;i<size;i++)
	{
		if(arr[i]%2=0)
		{
			{
		printf("the even numbers are %d",arr[i]);
		}else {
		printf("there is no even number")}
	}
	for(i=0;i<size;i++)
	{
		if(arr[i]%2!=0)
		printf("the odd numbers are:\n",arr[i])
		else
		printf("\nthere is no odd value")
	}
	
}